apiVersion: 1

datasources:
  # Prometheus (Métricas)
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090
    isDefault: true

  # Loki (Logs)
  - name: Loki
    type: loki
    url: http://loki:3100

  # Tempo (Trazas)
  - name: Tempo
    type: tempo
    url: http://tempo:3200
